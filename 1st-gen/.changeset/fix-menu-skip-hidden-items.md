---
'@spectrum-web-components/menu': patch
---

**fix(menu):** Fixed keyboard navigation in `sp-menu` landing on `sp-menu-item` elements that have the `hidden` attribute. Hidden items are now skipped the same way disabled items are.
