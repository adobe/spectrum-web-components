---
'@spectrum-web-components/reactive-controllers': minor
---

**Added**: `FocusGroupController` now supports a `stopKeyEventPropagation` config option. When set to `true`, arrow key events will stop propagation after being handled, preventing them from bubbling up to parent elements.
