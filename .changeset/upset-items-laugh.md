---
'@spectrum-web-components/action-menu': patch
---

**Replaced**: Dependency on `PickerBase` with `ExpandableElement`from `@spectrum-web-components/action-menu` to separate Action Menu from Picker.
