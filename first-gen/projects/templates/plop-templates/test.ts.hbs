import { fixture, elementUpdated, expect, html } from '@open-wc/testing';

import '@spectrum-web-components/{{name}}/{{> tagnamePartial }}.js';
import { {{className name}} } from '@spectrum-web-components/{{name}}';
import { testForLitDevWarnings } from '../../../test/testing-helpers.js';

describe('{{className name}}', () => {
    testForLitDevWarnings(async () => await fixture<{{className name}}>(
        html`
            <{{> tagnamePartial}}></{{> tagnamePartial}}>
        `
    ));
    it('loads default {{name}} accessibly', async () => {
        const el = await fixture<{{className name}}>(
            html`
                <{{> tagnamePartial}}></{{> tagnamePartial}}>
                `
            );
        
        await elementUpdated(el);

        await expect(el).to.be.accessible();
    });
});