import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="React wrappers" />

# React wrappers

This guide will help you understand the React wrappers for SWC.

## What are the React wrappers?

The React wrappers are a set of React components that help you use SWC in your React applications.

## What are the rules?

The rules are:
