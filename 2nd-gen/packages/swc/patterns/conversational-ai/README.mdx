import { Meta, Canvas } from '@storybook/addon-docs/blocks';
import * as ConversationTurnStories from './conversation-turn/stories/conversation-turn.stories';

<Meta title="Conversational AI/README" />

# Conversational AI

A complete set of building blocks for composing AI chat interfaces in 2nd-gen Spectrum Web Components. The pattern covers the full response lifecycle — from user input through AI generation, output, feedback, source attribution, and follow-up suggestions.

---

## Full example

<Canvas of={ConversationTurnStories.FullPattern} />

---

## Components

| Element                           | Description                                                                                                  |
| --------------------------------- | ------------------------------------------------------------------------------------------------------------ |
| `swc-prompt-field`                | Text input surface for entering or refining a prompt                                                         |
| `swc-conversation-artifact-card`  | Horizontal file-style attachment row (optional building block)                                               |
| `swc-conversation-artifact-media` | Media-first attachment; preview-only tile when title/subtitle omitted                                        |
| `swc-conversation-turn`           | Column alignment for one user or assistant turn                                                              |
| `swc-user-message`                | User message bubble (copy, card, or media content)                                                           |
| `swc-assistant-message`           | Assistant reply stack; **`message`** slot guidance is after the API table on the Assistant message docs page |
| `swc-response-status`             | Loading / generation-complete status indicator                                                               |
| `swc-message-feedback`            | Thumbs-up / thumbs-down feedback control                                                                     |
| `swc-message-sources`             | Collapsible numbered list of response sources                                                                |
| `swc-message-suggestions`         | Row of follow-up suggestion chips                                                                            |

---

## Pattern structure

```text
patterns/
  conversational-ai/
    README.mdx
    assistant-prose-demo.css
    conversation-artifact-card/
    conversation-artifact-media/
    prompt-field/
    user-message/
    response-status/
    message-feedback/
    message-sources/
    message-suggestions/
    conversation-turn/
    assistant-message/
    utils/
      icons/
```

---

## Tokens

All components resolve styles through `token()` calls that map to `var(--swc-*)` custom properties. No hard-coded colours or sizes — set the standard Spectrum token layer to theme the pattern globally.
