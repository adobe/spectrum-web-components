{
    "files.exclude": {
        "**/*.css.ts": { "when": "$(basename)" },
        "packages/*/src/spectrum-vars.json": true,
        "**/*.js.map": true,
        "**/*.js": { "when": "$(basename).ts" },
        "**/*.d.ts": { "when": "$(basename).ts" }
    }
}
