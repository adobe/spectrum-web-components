{
    "$schema": "../../node_modules/nx/schemas/project-schema.json",
    "name": "asset",
    "root": "packages/asset",
    "sourceRoot": "packages/asset/src",
    "projectType": "library",
    "tags": ["component"],
    "targets": {
        "build": {},
        "build:ts": {},
        "build:css": {},
        "build:watch": {},
        "test": {
            "executor": "nx:run-commands",
            "defaultConfiguration": "scope"
        },
        "validate": {
            "executor": "nx:run-commands",
            "dependsOn": ["lint", "test"]
        }
    }
}
