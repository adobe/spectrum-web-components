import * as React from "react";
import { createComponent } from "@lit-labs/react";
import { StatusLight as Component } from "@iliad-ui/bundle";

export const StatusLight = createComponent(
  React,
  "sp-status-light",
  Component,
  {},
  "StatusLight"
);
