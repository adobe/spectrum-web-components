import * as React from "react";
import { createComponent } from "@lit-labs/react";
import { ActionBar as Component } from "@iliad-ui/bundle";

export const ActionBar = createComponent(
  React,
  "sp-action-bar",
  Component,
  {},
  "ActionBar"
);
