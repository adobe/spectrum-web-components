{
    "$schema": "../../node_modules/nx/schemas/project-schema.json",
    "name": "tooltip",
    "root": "packages/tooltip",
    "sourceRoot": "packages/tooltip/src",
    "projectType": "library",
    "tags": ["component"],
    "targets": {
        "build": {},
        "build:ts": {},
        "build:css": {},
        "build:watch": {},
        "test": {
            "executor": "nx:run-commands",
            "defaultConfiguration": "scope"
        },
        "validate": {
            "executor": "nx:run-commands",
            "dependsOn": ["lint", "test"]
        }
    }
}
