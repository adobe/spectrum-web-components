const config = {
    spectrum: '{{spectrum}}',
    components: [
        {
            name: '{{ name }}',
            host: {
                selector: '.spectrum-{{className name}}',
            },
        },
    ],
};

export default config;