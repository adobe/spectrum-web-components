## Description



### Usage

[![See it on NPM!](https://img.shields.io/npm/v/@iliad-ui/{{ name }}?style=for-the-badge)](https://www.npmjs.com/package/@iliad-ui/{{ name }})
[![How big is this package in your project?](https://img.shields.io/bundlephobia/minzip/@iliad-ui/{{ name }}?style=for-the-badge)](https://bundlephobia.com/result?p=@iliad-ui/{{ name }})

```
yarn add @iliad-ui/{{ name }}
```

Import the side effectful registration of `<{{> tagnamePartial}}>` via:

```
import '@iliad-ui/{{ name }}/{{> tagnamePartial}}.js';
```

When looking to leverage the `{{className name}}` base class as a type and/or for extension purposes, do so via:

```
import { {{className name}} } from '@iliad-ui/{{ name }}';
```

## Example

```html
<{{> tagnamePartial}}></{{> tagnamePartial}}>
```