## Description



### Usage

[![See it on NPM!](https://img.shields.io/npm/v/@future-ui/{{ name }}?style=for-the-badge)](https://www.npmjs.com/package/@future-ui/{{ name }})
[![How big is this package in your project?](https://img.shields.io/bundlephobia/minzip/@future-ui/{{ name }}?style=for-the-badge)](https://bundlephobia.com/result?p=@future-ui/{{ name }})

```
yarn add @future-ui/{{ name }}
```

Import the side effectful registration of `<{{> tagnamePartial}}>` via:

```
import '@future-ui/{{ name }}/{{> tagnamePartial}}.js';
```

When looking to leverage the `{{className name}}` base class as a type and/or for extension purposes, do so via:

```
import { {{className name}} } from '@future-ui/{{ name }}';
```

## Example

```html
<{{> tagnamePartial}}></{{> tagnamePartial}}>
```