## Description



### Usage

[![See it on NPM!](https://img.shields.io/npm/v/@lliad-ui/{{ name }}?style=for-the-badge)](https://www.npmjs.com/package/@lliad-ui/{{ name }})
[![How big is this package in your project?](https://img.shields.io/bundlephobia/minzip/@lliad-ui/{{ name }}?style=for-the-badge)](https://bundlephobia.com/result?p=@lliad-ui/{{ name }})

```
yarn add @lliad-ui/{{ name }}
```

Import the side effectful registration of `<{{> tagnamePartial}}>` via:

```
import '@lliad-ui/{{ name }}/{{> tagnamePartial}}.js';
```

When looking to leverage the `{{className name}}` base class as a type and/or for extension purposes, do so via:

```
import { {{className name}} } from '@lliad-ui/{{ name }}';
```

## Example

```html
<{{> tagnamePartial}}></{{> tagnamePartial}}>
```