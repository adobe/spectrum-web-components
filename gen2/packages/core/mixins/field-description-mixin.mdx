import { Canvas, Meta } from '@storybook/addon-docs/blocks';
import { DocsFooter, DocsHeader } from '../../swc/.storybook/blocks';

import * as Stories from './stories/field-description-mixin.stories';

<Meta of={Stories} />

<DocsHeader />

## Usage

`FieldDescriptionMixin` adds description/error-text rendering and accessible description wiring to a host so components do not hand-roll it. It is one of two decoupled primitives for form-field labelling:

- **`FieldDescriptionMixin`** (`@adobe/spectrum-wc-core/mixins`): the `accessible-describedby` attribute (mirrored by the `accessibleDescribedby` property), `description` / `error-text` slot presence tracking, and `renderFieldDescription()`.
- **`renderFieldDescription`** (`@adobe/spectrum-wc-core/directives/render-field-description`): the underlying render-only directive. The mixin owns and exposes it; use it directly only for stateless rendering without the mixin.

`LabellingMixin` (`@adobe/spectrum-wc-core/mixins`) is the companion mixin for accessible-name association; `swc-text-field` composes both.

### What it does

- **Combined description sources**: unlike accessible-name sources, a slotted `description` and an external `accessible-describedby` reference combine rather than override each other. The role element's `ariaDescribedByElements` lists the in-shadow description first, then the resolved external elements.
- **Invalid-gated error message**: while the host reads as `invalid` the in-shadow error-text element replaces the description in that `ariaDescribedByElements` list (the description is hidden; any external elements remain). `invalid` is read structurally, so a host that doesn't declare it at all simply never surfaces an error message. It is not exposed via `aria-errormessage`, since AT support for that attribute is still inconsistent.
- **Conditional rendering**: `renderFieldDescription()` renders nothing when there is no description content and no active error message.

### Basic usage

1. Apply the mixin to your base class: `class MyField extends FieldDescriptionMixin(SpectrumElement) {}`.
2. Override `roleElement` to return the element the description/error-message relationships should be wired onto.
3. Call `renderFieldDescription()` in your template.

```typescript
import { html, LitElement } from 'lit';
import { FieldDescriptionMixin } from '@adobe/spectrum-wc-core/mixins';

class MyField extends FieldDescriptionMixin(LitElement) {
  invalid = false;

  override get roleElement() {
    return this.renderRoot.querySelector('input');
  }

  render() {
    return html`
      <input />
      ${this.renderFieldDescription()}
    `;
  }
}
```

## Behaviors

### Combined description sources

<Canvas of={Stories.CombinedDescription} />

### Error text replaces the description while invalid

While the host is `invalid`, the error message replaces the description: the error-text element is shown and associated via `ariaDescribedByElements` in place of the description, which is hidden. When the host returns to valid, the description is shown and re-associated.

<Canvas of={Stories.ErrorTextGating} />

## Accessibility

### Features

- **Error message replaces the description while invalid**: an invalid field shows its error message in place of the description rather than alongside it. `ariaDescribedByElements` references only the message currently on screen, so a screen reader never announces hidden description text, and the error is surfaced through `aria-describedby` rather than a separately-pointed `aria-errormessage`.
- **No default live region**: help/error content is wired through `ariaDescribedByElements`, not an `aria-live` region, so a screen reader announces it when focus lands on the field.

### Best practices

- Pair `invalid` with slotted `error-text` so the invalid state is not conveyed by color alone.
- Use `accessible-describedby` for a description composed from light-DOM content the field doesn't own; it combines with, rather than replaces, a slotted `description`.

<Canvas of={Stories.Accessibility} />

## API

### Constructor

`FieldDescriptionMixin(BaseClass)` returns `BaseClass` extended with the field-description API.

### Members

| Member                      | Description                                                                                                   |
| --------------------------- | ------------------------------------------------------------------------------------------------------------- |
| `accessibleDescribedby`     | Space-separated element `id`s that describe the role element. Combines with a slotted `description`.          |
| `hasDescriptionSlotContent` | Read-only. Whether slotted `description` content is present.                                                  |
| `hasErrorTextSlotContent`   | Read-only. Whether slotted `error-text` content is present.                                                   |
| `roleElement`               | `@internal`. The element the resolved description/error relationships are wired onto.                         |
| `renderFieldDescription()`  | Renders the description/error-text markup for the current state (via the `renderFieldDescription` directive). |

### Related primitives

| Export                   | Module                                                        | Purpose                                                                                  |
| ------------------------ | ------------------------------------------------------------- | ---------------------------------------------------------------------------------------- |
| `renderFieldDescription` | `@adobe/spectrum-wc-core/directives/render-field-description` | Underlying field-description render; exposed via the mixin's `renderFieldDescription()`. |
| `LabellingMixin`         | `@adobe/spectrum-wc-core/mixins`                              | Companion mixin for accessible-name association.                                         |

<DocsFooter />
