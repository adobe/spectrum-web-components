import { Canvas, Meta } from '@storybook/addon-docs/blocks';
import { DocsFooter, DocsHeader } from '../../../.storybook/blocks';

import * as PixelLoaderStories from './stories/pixel-loader.internal.stories';

<Meta of={PixelLoaderStories} />

<DocsHeader />

## Anatomy

A pixel loader consists of:

- A **grid of pixel cells** that drop and fade into place to assemble the
  active icon, then drop and fade away to disassemble it before the next loop

<Canvas of={PixelLoaderStories.Anatomy} />

## Options

### Icons

The catalog includes 30 icons which include a hand-authored logo mark plus
~29 generated glyphs. A representative sample is shown below. Check out the [Playground](?path=/story/patterns-ai-toolkit-pixel-loader--playground)
for all icons.

<Canvas of={PixelLoaderStories.Icons} />

### Presets

A preset cycles through a themed sequence of icons, one per loop, instead of
looping a single icon forever.

<Canvas of={PixelLoaderStories.Presets} />

### Sizing

The loader has no `size` attribute. Set its rendered size by overriding the
`--swc-pixel-loader-size` custom property in your own CSS (it defaults to
56px):

```css
swc-pixel-loader {
  --swc-pixel-loader-size: 24px;
}
```

## States

`paused` renders the fully-settled appearance (full size, full opacity, no
offset) with no animation. Under reduced motion the loader fades the grid in
and out one row at a time in place, without the falling or scaling motion (see
Accessibility).

<Canvas of={PixelLoaderStories.States} />

## Accessibility

### Features

The `<swc-pixel-loader>` element implements the following accessibility
features:

- Host renders with `role="progressbar"`, with no `aria-valuenow` /
  `aria-valuemin` / `aria-valuemax` since progress is always indeterminate
- The `label` property (default `"Loading"`) sets the accessible name via
  `aria-label`
- When the user has requested reduced motion, the loader drops the falling and
  scaling motion: it instead fades the grid in and out one row at a time
  (bottom-up) in place, so it still signals that something is loading while
  honoring the reduced-motion preference.

### Best practices

- Always provide a descriptive `label` that explains what's loading (for
  example `"Generating response"`) instead of relying on the default
  `"Loading"`
- Don't rely on the animation alone to convey that an operation is in
  progress for long-running operations; pair it with a visible status message
  elsewhere in the UI

<Canvas of={PixelLoaderStories.Accessibility} />

<DocsFooter />
